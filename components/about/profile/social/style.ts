import styled from 'styled-components';

import Grid from '@material-ui/core/Grid';
import { SocialIcon } from 'react-social-icons';

export const Container = styled(Grid)`
  padding: 10;
`;

export const SocialButton = styled(SocialIcon)`
  margin: 15px;
`;
