import styled from 'styled-components';

import Grid from '@material-ui/core/Grid';

export const ProfileListItem = styled(Grid)`
  color: #000000;
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  text-align: left;
  margin: 0;
  padding: 0;
`;

export const ProfileListItemValue = styled(Grid)`
  color: #000000;
  font-size: 15px;
  text-align: left;
  margin-top: -3px;
  padding: 0;
  font-weight: 700;
`;

export const ProfileValueAItemValue = styled.a`
  color: inherit;
  font-weight: 700;
`;
